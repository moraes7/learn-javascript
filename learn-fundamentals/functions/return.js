// Uma função pode retornar um valor de volta ao código como resultado.
function nomeCompleto(nome, sobrenome) {
    return `${nome} ${sobrenome}`
}

let id = nomeCompleto("Nicolas", "Moraes")
console.log(id);