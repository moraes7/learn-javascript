// A função prompt aceita dois argumentos: result = prompt(title, [default]);

let age = prompt("Quantos anos você tem?", 100);
alert(`Você tem ${age} anos de idade.`); // "Você tem 100 anos de idade"

let name = prompt("Qual é o seu nome?");
alert(`Bem vindo, ${name}!`) // "Bem vindo, Nicolas"